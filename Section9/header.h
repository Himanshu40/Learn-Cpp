#ifndef HEADER_H
#define HEADER_H

#include <vector>


void printElements(std::vector<int> &tempVec);
void addElement(std::vector<int> &tempVec);
void meanElements(std::vector<int> &tempVec);
void smallestElement(std::vector<int> &tempVec);
void largestElement(std::vector<int> &tempVec);
void findElement(std::vector<int> &tempVec);
void clearElement(std::vector<int> &tempVec);


#endif
